// Popup root. Settings + quick converter UI. Reads/writes settings via
// browser.storage and talks to the worker via typed messages. No direct network.

export default function App() {
  return <main className="popup">OpenConvert</main>;
}
