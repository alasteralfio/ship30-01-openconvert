// RateProvider interface plus implementations: open.er-api (primary) and
// Frankfurter (fallback). Each normalises to { base, rates, updatedAt, nextUpdate }
// so providers are drop-in swappable.

export {};
